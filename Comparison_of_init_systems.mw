{| class="wikitable"
!
!sysvinit / BSD init
!OpenRC
<!-- note: please keep sysvinit-based inits next to it -->
!upstart
!systemd
!SMF
|-
| Supported platforms || Linux / BSD || Linux + BSD || Linux || Linux || Solaris
|-
| Main coding language || C || POSIX shell (+ C) || C || C || ?
|-
| Main dependencies || - || init (sysv or BSD) || D-Bus || D-Bus || ?
|-
| Init script/service format || config file || shell scripts || config files || config files || XML (+ shell scripts)
|-
| Running as a daemon || yes (PID 1) || no || yes (PID 1) || yes (PID 1) || ?
|-
| Cross-service dependencies || no || yes || yes([http://netsplit.com/2010/05/27/dependency-based-event-based-init-daemons-and-launchd/ events]) || yes || yes
|-
| Parallel service startup || yes || yes (optional) || ? || yes || yes
|-
| Daemon auto-restart || yes || no || ? || yes || yes
|}
