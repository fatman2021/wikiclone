{{InfoBox stack
|{{InfoBox homepage|http://www.openbox.org|header=true}}
|{{InfoBox wikipedia}}
|{{InfoBox gdoc|openbox}}
|{{InfoBox todo|[[#Examples|Examples]]}}
}}
'''Openbox''' is a highly configurable, next generation window manager with extensive standards support. 

{{Note| This guide is not a replacement to the [http://www.gentoo.org/doc/en/openbox.xml official Gentoo Openbox Guide] at the moment. Its only purpose is to illustrate the changes between version 3.4 and 3.5 .}}

== Installation ==
Install {{Package|x11-wm/openbox}}:
{{USEflag
|package=x11-wm/openbox
|branding
|debug
|imlib
|nls
|python
|session+yes+yes
|startup-notification+yes
|static-libs
}}

{{Emerge|openbox}}

== Configuration ==
There are four files to consider when configuring Openbox:
* {{Path|autostart}}
* {{Path|environment}}
* {{Path|menu.xml}}
* {{Path|rc.xml}}

Openbox installs these files to {{Path|/etc/xdg/openbox/}}. If you want to override them, create your own in {{Path|~/.config/openbox/}}.

=== Starting ===
To start Openbox you can use a [[display manager]] or startx.

If want to use startx and want [[ConsoleKit]] support, setup ConsoleKit and create the following file:
{{File|~/.xinitrc||<pre>
exec ck-launch-session dbus-launch --sh-syntax --exit-with-session openbox-session
</pre>}}

=== Autostart ===
The {{Path|autostart}} file is the ideal way to execute specific commands and programs when Openbox starts. It makes no difference if you start Openbox using '''startx''' or any display manager. Here is an example:
{{File|~/.config/openbox/autostart||<pre>
wallpaper="planet.jpeg"
DESKTOP_ENV="OPENBOX"
conky --daemonize
sleep 1 && nm-applet --sm-disable &
fbpanel &
feh --bg-scale ~/Artwork/${wallpaper}&
batti &
numlockx &
</pre>}}

=== Environment ===
This is the ideal place to export global variables and configure your Openbox environment as appropriate:
{{File|~/.config/openbox/environment||<pre>
eval $(gpg-agent --daemon)
eval $(gnome-keyring-daemon)
if which dbus-launch >/dev/null && test -z "$DBUS_SESSION_BUS_ADDRESS"; then
       eval `dbus-launch --sh-syntax --exit-with-session`
fi
LANG="el_GR.UTF8"
</pre>}}

=== menu.xml ===

=== rc.xml ===
{{Path|rc.xml}} is used to specify keybindings and mousebindings. It is located in {{Path|/etc/xdg/openbox}} or {{Path|~/.config/openbox}}.

The following are the modifiers.
{| class="wikitable sortable"
|-
! Key !! Description
|-
| S || Shift key
|-
| C || Control key
|-
| A || Alt key
|-
| W || Super (windows) key
|-
| M || Meta key
|-
| H || Hyper key
|}

To make a keybinding, you can combine modifiers and a key. They are separated with the '-' sign.
{{Important| Case matters. 'S' (uppercase) is the shift key while 's' (lowercase) is the s alphabet key.}}

==== Examples ====
{| class="wikitable"
|-
! Keybinding !! Modifier
|-
| Alt-o || A-o
|-
| Ctrl-Alt-x || C-A-x
|-
| Ctrl-Alt-Shift-y || C-A-S-y
|-
| Shift-s || S-s
|}

{{Important| The keybinding should go between ''<keyboard>'' and ''</keyboard>'' in {{Path|rc.xml}}.}}

If you want to open xterm with '''Super-t''', you should add the following to the {{Path|rc.xml}}:
{{File|~/.config/openbox/rc.xml||<pre>
<keybind key="W-t">
  <action name="Execute">
    <command>xterm</command>
  </action>
</keybind>
</pre>}}

You can also bind menus. For example, you can bind Openbox right click and middle click menu with the following:
{{File|~/.config/openbox/rc.xml||<pre>
<keybind key="W-x">
  <action name="ShowMenu">
    <menu>root-menu</menu>
  </action>
</keybind>

<keybind key="W-z">
  <action name="ShowMenu">
    <menu>client-list-combined-menu</menu>
  </action>
</keybind>
</pre>}}

In the above example, the right click menu is bound with Super-x, and the middle click menu is bound with Super-z. It is useful especially if you prefer not to use any panel with Openbox.

TODO

== Tips ==

=== Icons in Openbox menu ===
Since version 3.5.0 you can have icons next to your menu entries. For this:

1) You must emerge openbox with imlib support:
Add USE flag ''imlib'' for x11-wm/openbox into you {{Path|/etc/portage/package.use}} like this:
{{RootCmd|echo "x11-wm/openbox imlib" >> /etc/portage/package.use}}
{{Emerge|params+=--changed-use|openbox}}

2) Add ''<showIcons>yes</showIcons>'' in the ''<menu>'' section of the {{Path|rc.xml}} file.

3) Add in ''menu.xml icon="<path>"'' like this:
{{File|menu.xml||<pre>
<menu label="Shells" icon="/usr/share/icons/shell.png">
	<item label="xterm" icon="/usr/share/icons/xterm.png"><action name="Execute">
		<execute>xterm</execute>
	</action></item>
</pre>}}

== See also ==
* [[Tint2]] - A lightweight panel/taskbar

[[Category:Window manager]]
