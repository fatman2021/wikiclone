Bash is the default shell for gentoo linux.

== Keyboard shortcuts ==
<pre>
Ctrl + C - Kill whatever you are running
Ctrl + D - Exit the current shell
Ctrl + L - Clears the Screen like the clear command
Ctrl + R - Let’s you search through previously used commands
Ctrl + A - Go to the beginning of the line you are currently typing on
Ctrl + E - Go to the end of the line you are currently typing on
Ctrl + Z - Puts whatever you are running into a suspended background process. fg restores it.
Tab - Auto-complete files and folder names
</pre>

== Configuration ==
Example configuration file.
{{file|~/.bashrc||<pre>
if [[ $- != *i* ]] ; then
	# Shell is non-interactive.  Be done now!
	return
fi

# Enable bash completion
if [ -f /etc/profile.d/bash-completion.sh ]; then
	. /etc/profile.d/bash-completion.sh
fi

# Customize prompt
export PS1='[\[\033[01;32m\]\u@\h\[\033[00m\]]─[\[\033[01;34m\]\w\[\033[00m\]]\n>\[\033[1;32m\] \[\033[00m\]'

# Colorized man pages
export MANPAGER=/usr/bin/most

# Command aliases
alias dir="du -hs * .??* | sort -hr"    # list files and folders in size order
alias reload="source ~/.bashrc && xrdb -load ~/.Xresources"
alias blank="echo \"Blanking monitor in 3 seconds..\" && sleep 3 && xset dpms force standby"
alias df="df -h"
alias mkdir="mkdir -p"
alias cp="cp -v"
alias mv="mv -v"
alias rm="rm -iv"
</pre>}}
