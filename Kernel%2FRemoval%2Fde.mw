<languages />
Dieser Artikel beschreibt die Deinstallation von alten [[Special:MyLanguage/Kernel|Kerneln]].

== Kernelquellen deinstallieren  ==

Hat man einen neuen Kernel installiert und läuft dieser zufriedenstellend, so kann der alte Kernel entfernt werden. Um die alten Kernelquellen zu deinstallieren, bietet [[Special:MyLanguage/emerge|emerge]] die Option ''--depclean'' (Kurzform ''-c''), um all alten und ungenutzten Versionen eines Paketes mit Slots zu deinstallieren, z.B. bei {{Package|sys-kernel/gentoo-sources}}:

{{RootCmd|emerge --depclean --ask gentoo-sources}}

Man sollte sich vergewisseren, dass der Befehl nicht die aktuell verwendeten Kernelquellen deinstalliert. (Siehe dazu auch den [[Special:MyLanguage/Kernel/Upgrade|Kernel Upgrade]] Artikel.)

== Kernelreste entfernen ==

=== eclean-kernel verwenden ===

{{Package|app-portage/eclean-kernel}} ist ein einfaches Hilfsprogramm zum Säubern alter Kernel. Es entfernt sowohl die Kerneldateien als auch temporäre Verzeichnisse, wenn sie nicht mehr von den gewünschten Kerneln  genutzt werden.

Um z.B. nur die drei neuesten Kernel zu erhalten:

{{RootCmd|eclean-kernel -n 3}}

=== Manuelles Entfernen ===

Portage entfernt nur die Dateien, die es selber installiert hat. Dateien, die beim Kompilieren und Installation des Kernels erstellt wurde, verbleiben im System. Sie können jetzt aber auch entfernt werden.

* Wenn ein Kernel im Kernelverzeichnis kompiliert worden ist, verbleiben dort die temporären Dateien auch nach der Deinstallation durch Portage:

: {{RootCmd|rm -r /usr/src/linux-2.6.XY}}

Bei der Installation des Kernel werden die Module in ein Unterverzeichnis von /lib/modules/ kopiert:

: {{RootCmd|rm -r /lib/modules/2.6.XY}}

* Die alten Dateien in <tt>/boot</tt> können auch entfernt werden:

: {{RootCmd|rm /boot/kernel-2.6.XY|rm /boot/System.map-2.6.XY|rm /boot/config-2.6.XY}}

* Lastly, remove all leftover entries from your bootloader's config file.

[[Category:Kernel]]
