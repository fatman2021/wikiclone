I add to run the following lines :

{{RootCmd
|cd /bin ; ln -s sh bash ;
|cd /dev ; ln -s /proc/self/fd fd}}

Pour la compilation de la sandbox :
{{RootCmd
|echo "portage:x:250" >> /data/gentoo/etc/group}}
